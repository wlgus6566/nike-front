<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>
<configuration scan="true" scanPeriod="30 seconds">

	<property name="LOG_PATH" value="C:/Users/emotion/dev/log"/>
	<property name="LOG_FILE" value="catalina.log"/>

	<appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
        	<charset>UTF-8</charset>
            <pattern>%d{yyyy.MM.dd HH:mm:ss.SSS} [%-3level] ---%-40.40logger{39} : %msg%n}</pattern>
        </encoder>
        <file>${LOG_PATH}/${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}-%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern>
			<maxFileSize>100MB</maxFileSize>
            <maxHistory>15</maxHistory>
        </rollingPolicy>
    </appender>
	
	<!-- Loggers -->
	<logger name="org.springframework" level="INFO" />
	<logger name="com.nike.dnp" level="INFO" />
	<logger name="jdbc.sqlonly" level="INFO" />

	<root level="ERROR">
		<appender-ref ref="ROLLING" />
	</root>

</configuration>