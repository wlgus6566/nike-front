module.exports = {
    plugins: [
        /*require('postcss-import')({
            path: ['app/assets/'],
        }),*/
        require('autoprefixer'),
        require('postcss-flexbugs-fixes'),
    ],
};
